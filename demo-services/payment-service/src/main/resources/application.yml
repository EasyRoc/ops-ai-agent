server:
  port: 8082

spring:
  application:
    name: payment-service

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,metrics
  metrics:
    tags:
      service: payment-service
